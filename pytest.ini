[pytest]
testpaths = tests
addopts = --basetemp=.tmp/pytest -p no:cacheprovider --import-mode=importlib
markers =
    e2e: original L3 three-service acceptance requiring ports 8100-8102
    browser: isolated UI component tests
    requires_real_business: original test requiring the real product workbook

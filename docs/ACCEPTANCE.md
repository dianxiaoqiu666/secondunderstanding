# 历史 M05 自主交付验收

当前验收边界、真实文件操作与证据见 [M06 修复与复验](M06_DELIVERY.md)。下文保留历史自动检查，不能代替本轮真实用户验收，也不能把合成完整/缺口输入称为真实门店通过。

负责人最终实际运行：

```powershell
.venv\Scripts\python.exe -B -m pytest -q --basetemp=.tmp/pytest-m05-final --junitxml=outputs/acceptance_m05/junit.xml
```

**337 passed、17 subtests passed，119.79秒，2项依赖弃用警告。** JUnit计数354、失败/错误/跳过均0。包含60项明确历史外壳回归；总数不能替代下述操作负担与实际交付证明。随后仅澄清必要入口/孔洞勾选文案，负责人另行实际启动并用Chromium核对当前网页和已交付真实三维场景，烟雾检查PASS，记录live-smoke.json。其首轮测试谓词误写完成标签，按实际页面“规划完成”修正后通过；产品状态无异常。

## 人工操作负担：真实浏览器执行

所有流程从网页选择真实磁盘DXF、点击开始，没有向页面注入已确认状态，没有mock成功响应。三个实际微服务由start.ps1启动，Python子进程阻断非回环网络与原始参考目录，浏览器阻断外网；最终WebGL网格和下载文件实际读取验证。

| 样例 | 额外操作 | 实际结果 |
|---|---:|---|
| 明确完整输入（标明合成测试） | 0 | 自动理解→规划/品类→检查→3D/JSON/XLSX；无编辑面板 |
| 明确scope仅100mm源端点缺口（合成） | 2 | 选择已有两个源端点的选项并提交；不绘整圈、不显示五项勾选，自动运行到底 |
| 上述已补文件同字节重传 | 0 | 签名事实复用，完整布局JSON逐字节一致，无重复确认 |
| 唯一获批真实CAD | 总操作数UNKNOWN | 已执行额外操作0，未完成；具体缺3项事实，不能计为“零操作交付成功” |

根实际任务ID：完整a59c8eb6442842f5b8d63aae4cf9b22f；局部efa4e857f79e4eeabd474f371b16c708；复用df63fbf0ef1d4e83bc41588cbdce34c5；真实待补充d2d96dfbeeac44ac94dae6f4432fc4ad。

## 实际交付与几何证据

三份已完成测试图均65个模块、10条连续排、默认层板445；10个1200×400默认6层、55个1800×400默认7层，6排混合规格。3D中65个实际货架逻辑对象及445层板实例，与XLSX/BOM完全一致。地面真实三角网格面积213750000mm²，等于216m²范围减2.25m²孔洞；未以hole_count字段替代拓扑检查。墙、禁放区、入口及所有模块均在场景中验证。

实际鼠标操作验证旋转、滚轮缩放、右键平移、重置、俯视和raycaster点选规格。真实下载layout JSON与标准结果全等；XLSX逐规格核对长度/深度/默认层数/数量/总层数，并核对实例表。文本写作字符串，不执行外来名称公式。真实SKU仍缺物理尺寸，只有源品类分配。

连续排独立重建55处接缝全0、孤立率0、10对平行排净距均1200mm，生产完整几何/BOM/排端检查通过。独立成熟参考对照见M05_STRUCTURAL_COMPARISON.md；参考120已核验模块、44严格排/36诊断排，真实面积未知，跨店数量/密度不设伪造通过阈值。

## 实际源仍缺什么

获批CAD SHA bd92b40c74cbbe75001b470f20afbaa3ca4aa3da0d2cb425d98d8c1d015defd0：缺可靠真实边界、明确入口，以及图层0的155/180/181源图元语义。外围粗框无可信局部补线候选；系统不能自动闭合/修复它，也不能借成熟图坐标补答案。

页面只展示这些具体事实，可一次集中补充。提交后自动恢复全流程，不要求回聊天或阶段审批。真实界外架数、几何违规、最终3D仍UNKNOWN/未生成；项目整体PARTIAL仅保留这项真实输入与交付限制。

## 产物与保护

outputs/acceptance_m05/保存JUnit、四份样例操作报告、三份最终JSON/XLSX、真实3D/页面截图和离线守卫标记。独立结构快照outputs/iterations/M05/reference_comparison.json的6个输入文件SHA已由负责人复核一致。源复验34分类路径匹配、37原件SHA/大小/修改时间全部不变，outputs/source_audit_final.json=PASS。

测试与实际产品目标分开：M05自主流程验收PASS，真实原CAD完整交付未完成。旧M04报告归档于_archive/M04_MANUAL_WORKBENCH/docs，旧FAILED_BASELINE_01仍保留。

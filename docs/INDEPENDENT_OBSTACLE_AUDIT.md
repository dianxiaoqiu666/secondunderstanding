# M02 独立障碍算法审计

状态：M02 五组实现完成后，独立完整执行 **113 passed in 25.12s**（`tests/audit_v2` + `tests/planning_v2`），其中本文件对应的独立障碍用例 **27/27 PASS**。这是合成障碍算法验收，不是正式门店或产品验收。

范围仅为 `tests/audit_v2/test_obstacle_audit.py` 和本报告；审计角色不修改生产算法。

输入全部由测试内独立构造，既不读取正式 CAD，也不读取 REFERENCE_ONLY。案例包括额外尺寸的柱、内墙、斜墙、孔洞、入口、凹形范围、入口与孔洞和柱的组合、约 10^8 mm 平移，以及障碍中间只能容纳短碎排的情况。

验收谓词独立于规划器内部裁剪方法：逐个检查真实模块轮廓与范围、孔洞、墙和禁放／入口的关系；检查任意两排净距；从排两端构造完整 `end_aisle_mm` 通行矩形，检查其内部没有墙、孔洞、实体障碍及其他排。入口净空允许用于通行，但不得摆货架。只验证局部排端通行，不声称全店入口可达性或消防认证。

另包括非法空范围、空孔洞、自交孔洞、范围外孔洞、空禁放区孔洞、自交禁放区、退化墙，以及字典和已实例化请求两种形式的负规则。要求返回明确领域错误或数据验证错误，不能静默接受或以 IndexError 等未处理异常代替。

重点静态疑点已关闭：墙退让与边界退让不同，原先统一使用 `end_aisle_mm - boundary_clearance_mm` 扩展排端可能少留实际过墙距离。修复将墙最小安全退让设为墙／边界退让两者的较大值，再补足端通道，并在验证器中直接检查真实通行矩形。专用测试使用边界退让 300 mm、墙退让 50 mm、排端通道 1200 mm，实际通过。

运行命令（项目根目录）：

```powershell
.\.venv\Scripts\python.exe -B -m pytest tests/audit_v2 tests/planning_v2 -q -o addopts='-p no:cacheprovider --import-mode=importlib'
```

本次独立复核了 `git diff -- services/planning/runs.py` 的主要安全边界：

- 输入的 Pydantic 实例也重新序列化验证；原地篡改为负规则不能绕过约束。
- boundary、hole、zone 均拒绝空／未闭合／非法拓扑，退化墙线显式拒绝；空 zone hole 不再产生未处理 IndexError。
- 候选区由真实范围（含 holes）与墙／禁放／入口障碍做 GEOS 差集，再按全排深度切分可用区间；不以外包矩形冒充真实范围。
- 同排模块继续首尾相接；障碍切分后的短区间不满足最少模块数即舍弃。独立中间短碎用例确认左右有效排仍保留，中间不足区间没有被孤体填充。
- 验证器重建实际模块／整排轮廓，直接拒绝孔洞、墙和禁放／入口接触；独立验证全部排间净距和真实排端通道，模块及 BOM 身份仍一致。
- 未新增正式 CAD、REFERENCE_ONLY 路径或 I/O。原独立直接 I/O 静态检查仍通过。

本轮 27 项由 7 种障碍结构、3 个大坐标平移、1 个不同退让参数排端通道、1 个中间短碎排、8 个负规则输入、7 个非法几何输入组成；全部通过。原矩形和 DP 审计一起运行，无测试回归。

生产 HTTP 确认、真实人工确认、成熟设计比较及最终浏览器交付不属于本合成算法审计的 PASS 范围。全店入口可达性尚未由本局部通道验证证明，不能从 `end_aisles=PASS` 推导出来。
